require 'spec_helper'

describe Multilingual do
  it 'has a version number' do
    expect(Multilingual::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
