require 'spec_helper'

describe MultilingualCli do
  it 'has a version number' do
    expect(MultilingualCli::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
